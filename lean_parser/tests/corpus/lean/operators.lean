def add_mul_prec := a + b * c
def arrow_is_right_assoc := a -> b -> c
def sub_is_left_assoc := a - b - c
def pow_is_right_assoc := a ^ b ^ c
def a_comparison := a = b
def append_and_cons := a ++ b :: cs
def logic_ops := p /\ q \/ not_p
def compose := f ∘ g
def product_and_sum := a × b
def unary_minus_binds_tight := -a ^ b
def negation := ¬ p
def iff_form := p <-> q
